12. 3 Processes running:
Django

$ python manage.py runserver
Celery Worker & Beat (this can be 2 processes)

# celery worker & beat 
$ celery -A cfehome worker --beat -S django -l info

opt.2 : celery -A djcelbeat beat  -l info   
redis server

$ redis-server



HEROKU - CELERY
>heroku addons:create heroku-redis:hobby-dev -a periodiccelery

# remove specific file from git cache
git rm --cached filename

# remove all files from git cache
git rm -r --cached .
git add .
git commit -m ".gitignore is now working"